# This package is retired. Toldya no longer runs a direct oracle signer.
# Set ToldyaHub's ORACLE_ADDRESS to the deployed Veto proxy, then run the Veto
# answerer/judge agents from the Veto repository.

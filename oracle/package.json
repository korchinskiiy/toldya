{
  "name": "toldya-oracle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI agent that resolves Toldya markets by reading the question + criteria and posting RESULT:YES or RESULT:NO on-chain.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "dotenv": "^16.4.5",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}

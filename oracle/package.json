{
  "name": "toldya-oracle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Retired direct oracle package. Toldya now resolves markets through the configured Veto contract.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p .",
    "typecheck": "tsc -p . --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
